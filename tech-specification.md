Веб-додаток "Аналізатор фраз та слів"

1. Мета проєкту

Створити веб-додаток, який дозволяє вводити текст, аналізувати його (кількість символів, слів), виділяти окремі слова, отримувати список синонімів із зовнішнього API та замінювати виділені слова на вибрані синоніми.

2. Функціональні вимоги
   2.1. Поле вводу

Використовується <textarea> (plain text, без форматування).
Текст може бути будь-якої довжини (до 100 000 символів).
В режимі реального часу повинні оновлюватися:
кількість символів
кількість слів

Слова рахуємо через алгоритм:

trim()
split(/\s+/)
filter(Boolean)

Підтримуються багаторядкові тексти.

2.2. Виділення слова / фрази

Система повинна вміти отримувати виділений текст через

selectionStart
selectionEnd
textarea.value.substring(...)
и виводити цей текст в консоль

Умови:
створити кнопку отримати синоніми
Якщо нічого не виділено → кнопка "Отримати синоніми" неактивна.

<!-- Максимальна довжина виділення для запиту: до 20 символів -->

кілька слів (фразу)

2.2.1. Нормалізація виділеного тексту

Перед відправкою у API:
Обрізаємо зайві пробіли.
Якщо виділення містить пунктуацію на краях (.,!?;:"'()), пунктуація видаляється.

2.3. Отримання синонімів
2.3.1. API

Використовується Datamuse API:
https://api.datamuse.com/words?rel_syn=WORD

2.3.2. Обмеження

Якщо виділене слово містить пробіли → робимо запит як фразу (Datamuse може повернути 0 результатів, це коректно).

Якщо слово не англійське — API поверне пустий список.
У такому випадку відображається повідомлення:
"Для цього слова не знайдено синонімів".

2.3.3. Поведінка кнопки

Поки триває запит → кнопка у стані loading (спінер).
Якщо запит успішний → показати список результатів.

Якщо помилка → показати:
"Помилка підключення до сервера. Спробуйте пізніше."

2.4. Відображення синонімів

Список відображається:
📍 У панелі справа від textarea (фіксована позиція, адаптив для мобільних → панель під textarea).
Кожен синонім — кнопка/тег.
Показувати максимум 20 варіантів.

Сортувати за полем score (з більшого до меншого).

2.5. Заміна слова у тексті

При кліку на синонім:

Текст у textarea змінюється:

newText =
textBeforeSelection

- synonym
- textAfterSelection

Статистика оновлюється.
Панель синонімів очищається.

2.6. Кнопка "Скопіювати весь текст"

Використовується navigator.clipboard.writeText(textarea.value).
Після успішного копіювання → toast-повідомлення:
"Текст скопійовано".
У разі помилки → повідомлення:
"Не вдалося скопіювати текст".

3. Нефункціональні вимоги
   3.1. Продуктивність

Система повинна коректно працювати з текстами до 100 000 символів.
Статистика оновлюється без затримок (використовується debounce 150 мс).

3.2. UX-вимоги

Кнопка «Отримати синоніми» неактивна, якщо нічого не виділено.
Після заміни синонімом виділення знімається.
Список синонімів зникає після натискання на будь-яку опцію.

3.3. Обробка помилок

Якщо API повернув пустий список → “Синоніми не знайдені”.
Якщо API недоступне → повідомлення про помилку.
Помилки не мають зупиняти роботу textarea.

4. Адаптивність

Десктоп:

textarea зліва → панель синонімів справа.

Мобільні:

textarea зверху → панель синонімів під textarea.

Кнопки дій (синоніми / копіювати) розташовані над textarea.
